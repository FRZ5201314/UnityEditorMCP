# unity2019-mcp-server

MCP stdio server for the Unity 2019 MCP Editor bridge.

```bash
npm install
npm run build
node dist/index.js
```

The Unity bridge must be running at `http://127.0.0.1:8765` unless overridden with `UNITY_MCP_BRIDGE_URL`.
